import {
  StyleSheet,
} from 'react-native';

const mainColor = '#143441';

export { mainColor };

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  paddingContainer: {
    flex: 1,
    padding: 30,
  },
});
